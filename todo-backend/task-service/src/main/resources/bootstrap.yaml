spring:
  cloud:
    config:
      uri: http://localhost:8088

  application:
    name: task-service

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: "*"








